---
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<div class={`text-block text-lg leading-relaxed text-gray-500 dark:text-gray-300 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 ${className}`}>
  <slot />
</div>
